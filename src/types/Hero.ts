export interface HeroDef {
  id: string;
  name: string;
  title: string;
  maxHp: number;
  gridCols: number;
  gridRows: number;
  startingItems: string[];
  passive: string;
  glyph: string;
  color: string;
}
